import { connect } from 'react-redux';

function Footer(){
    return(
        <nav>
            Footer
        </nav>
    )
}

const mapStateProps=state=>({

})

export default connect(mapStateProps,{

}) (Footer);