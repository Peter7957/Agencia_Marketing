import { connect } from 'react-redux';

function Navbar(){
    return(
        /** con py  puedo ver cuanto es el alto del div */
        <nav className='w-full py-6 shadow-md fixed'>
            <div>
                <link></link>
            </div>
        </nav>
    )
}

const mapStateProps=state=>({

})

export default connect(mapStateProps,{

}) (Navbar);